//Se crea una funcion que nos va a servir para buscar todos los clientes disponibles y no disponibles y los mostrara por pantalla
//Tiene que retornar unarray de objetos, con la fecha, hora, nombre del cliente, documento del cliente

//Construir una clase que se llame, turno, y que tenga como atributo, fecha, hora, nombre, apellido, documento

class Turno {
    constructor(fecha, hora, nombre, apellido, documento) {
        this.fecha = fecha;
        this.hora = hora;
        this.nombre = nombre;
        this.apellido = apellido;
        this.documento = documento;
    }
}

function conseguirTurnos() {
    // Your code here
    
}